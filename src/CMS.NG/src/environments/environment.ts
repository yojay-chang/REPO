// Production defaults. Replaced by environment.development.ts during `ng serve`
// (see fileReplacements in angular.json). Imported via the `@environments/environment` alias.
export const environment = {
  production: true,
  apiUrl: '/api',
};
