// Local development. The API runs on http://localhost:5000 (see CMS.API launchSettings).
// No dev-server proxy is used — the API base URL is read directly from here.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api',
};
